import style from "./ContactWrapper.module.css"

const ContactWrapper = () => { 
    return ( 
        <section className={style.contactWrapper}>
        <span> Contact Wrapper </span>
        </section>
    )
}

export default ContactWrapper